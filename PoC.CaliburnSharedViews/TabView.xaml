<UserControl x:Class="PoC.CaliburnPopup.TabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PoC.CaliburnPopup"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type={x:Type local:TabViewModel}, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300"
             cal:Bind.AtDesignTime="True">
    <Grid>
        <ContentControl cal:View.Model="{Binding Shared}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
